---
phase: 01-project-foundation
plan: 05
type: execute
wave: 1
depends_on: []
files_modified: [".eslintignore"]
autonomous: true
gap_closure: true
requirements: [SETUP-06]
must_haves:
  truths:
    - "`pnpm lint` exits 0 with zero bytes on stderr"
    - "`pnpm lint 2>&1 | grep ESLintIgnoreWarning` returns empty (exit 1 means no match)"
  artifacts:
    - path: ".eslintignore"
      provides: "DELETED — no longer present in the repo"
      note: "All patterns were already covered by the ignores block in eslint.config.js"
  key_links:
    - from: "eslint.config.js"
      to: "dist/**, node_modules/**, coverage/**"
      via: "ignores block (line 53)"
      pattern: "ignores:.*dist.*node_modules.*coverage"
---

<objective>
Delete the redundant `.eslintignore` file to eliminate the ESLintIgnoreWarning emitted by ESLint v9 on every `pnpm lint` run, satisfying SETUP-06's zero-warnings requirement.

Purpose: ESLint v9 flat-config mode does not support `.eslintignore`. Its presence causes ESLint to emit `ESLintIgnoreWarning: The '.eslintignore' file is no longer supported. Switch to using the 'ignores' property in 'eslint.config.js'.` to stderr on every invocation. The file is entirely redundant — `eslint.config.js` line 53 already has `ignores: ['dist/**', 'node_modules/**', 'coverage/**', 'examples/**', 'bench/**', '*.config.*']`, which is a superset of the three patterns in `.eslintignore` (`dist/`, `node_modules/`, `coverage/`).

Output: `.eslintignore` deleted. `pnpm lint` exits 0 with zero stderr output.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete .eslintignore and verify clean lint</name>
  <files>.eslintignore</files>
  <read_first>
    - `.eslintignore` — confirm it exists and its patterns (`dist/`, `node_modules/`, `coverage/`)
    - `eslint.config.js` line 53 — confirm `ignores` block covers all three patterns as a superset
  </read_first>
  <action>
    1. Confirm `.eslintignore` exists and contains only the three patterns `dist/`, `node_modules/`, `coverage/`.
    2. Confirm `eslint.config.js` already has `ignores: ['dist/**', 'node_modules/**', 'coverage/**', ...]` (line 53) — making `.eslintignore` fully redundant.
    3. Delete `.eslintignore` using the Bash tool: `rm /home/nschatz/projects/cosyte/hl7-mllp/.eslintignore`
    4. Run `pnpm lint 2>&1` from the project root and verify zero output on stderr (no ESLintIgnoreWarning).

    Do NOT modify `eslint.config.js` — the ignores block is already correct and complete. This task is a single file deletion only.
  </action>
  <acceptance_criteria>
    - `pnpm lint 2>&1 | grep -c ESLintIgnoreWarning` returns `0`
    - `pnpm lint` exits 0
    - `ls .eslintignore` returns a non-zero exit code (file does not exist)
  </acceptance_criteria>
  <verify>
    <automated>rm /home/nschatz/projects/cosyte/hl7-mllp/.eslintignore && cd /home/nschatz/projects/cosyte/hl7-mllp && pnpm lint 2>&1 | grep -c ESLintIgnoreWarning || true; pnpm lint; echo "exit:$?"</automated>
  </verify>
  <done>`.eslintignore` is deleted. `pnpm lint` exits 0 with zero lines on stderr. `pnpm lint 2>&1 | grep -c ESLintIgnoreWarning` returns `0`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Developer workstation → ESLint | ESLint reads config files from the repo root; no external network boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-05-01 | Tampering | `.eslintignore` deletion | accept | Single-file deletion with no security surface; ESLint config remains intact in `eslint.config.js`. |
</threat_model>

<verification>
After task completion:

```bash
# File no longer exists
ls /home/nschatz/projects/cosyte/hl7-mllp/.eslintignore
# Expected: ls: cannot access '.eslintignore': No such file or directory (exit 1)

# Lint is clean — zero stderr output
cd /home/nschatz/projects/cosyte/hl7-mllp && pnpm lint 2>/tmp/lint_stderr.txt; cat /tmp/lint_stderr.txt | wc -l
# Expected: 0

# Grep for the specific warning — must find nothing
pnpm lint 2>&1 | grep -c ESLintIgnoreWarning
# Expected: 0
```
</verification>

<success_criteria>
- `.eslintignore` does not exist in the repository
- `pnpm lint` exits 0
- `pnpm lint 2>&1 | grep ESLintIgnoreWarning` returns empty output (grep exits 1 = no match)
- `eslint.config.js` is unchanged
- SETUP-06 is fully satisfied: zero warnings from `pnpm lint`
</success_criteria>

<output>
After completion, create `.planning/phases/01-project-foundation/01-05-SUMMARY.md`
</output>
